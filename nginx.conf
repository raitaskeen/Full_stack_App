# Navigate to the Full_stack_App directory
cd D:\Full_stack_App

# Check if nginx.conf is a file
if [ -f nginx.conf ]; then
    echo "nginx.conf is a file."
else
    echo "nginx.conf is not a file."
fi

# If nginx.conf is not a file, create it
if [ ! -f nginx.conf ]; then
    touch nginx.conf
fi